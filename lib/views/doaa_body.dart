import 'package:flutter/material.dart';

class DoaaBody extends StatelessWidget {
  static const String id = 'doaa_body';
  const DoaaBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: const Center(
          child: Text('Doaa Body'),
        )
    );
  }
}