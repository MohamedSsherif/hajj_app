import 'package:flutter/material.dart';

class MnaskBody extends StatelessWidget {
  const MnaskBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: const Center(
          child: Text('Mnask Body'),
        )
    );
  }
}